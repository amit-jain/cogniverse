fastapi==0.120.1
uvicorn[standard]==0.44.0
pydantic==2.13.0
pillow==11.0.0
httpx==0.27.2
numpy==1.26.4
# InsightFace bundles RetinaFace (detector) + ArcFace (recogniser).
# 'buffalo_l' model pack ~210 MiB on disk; downloaded by FaceAnalysis
# on first prepare() call.
insightface==0.7.3
# InsightFace transitively needs onnxruntime for inference.
onnxruntime==1.20.1
