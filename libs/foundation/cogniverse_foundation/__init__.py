"""
Cogniverse Foundation Package.

Cross-cutting concerns and shared infrastructure used across all Cogniverse packages.

Modules:
- config: Configuration management (ConfigManager, unified config system)
- telemetry: Telemetry and observability (TelemetryManager, metrics)
- cache: Caching infrastructure (pipeline cache, backends)
- utils: Common utilities (logging, retry, async helpers)
"""

__version__ = "0.1.0"
