{{- /*
One nightly DR snapshot CronWorkflow per service in
``hostStorage.backup.services``. Each runs two steps: dump a tarball of
the service's data into a workflow-scoped staging volume, then mc cp it
to s3://<bucket>/<svc-name>/<timestamp>.tar and prune all but the most
recent ``retainLast`` snapshots.

Three dump modes are supported per service:

  mode: kubectl-exec   (default) — kubectl-exec into the live pod and
    tar the configured ``dataPath`` from inside. Works only for source
    containers that have ``tar`` and a POSIX shell on PATH (vespa).
    Cross-cuts hostStorage / PVC — works in either mode because it
    doesn't touch the volume directly.

  mode: volume-mount   — mount the source volume directly into the
    workflow pod, tar from local fs. Required for distroless source
    containers (phoenix's arizephoenix/phoenix image). RWO PVCs can't be
    backed up while the source pod is running — either use
    ReadWriteMany / ReadOnlyMany volumes, take a CSI VolumeSnapshot
    first, or scale the source pod to 0 during the backup window.

  mode: postgres — a ``pg_dump`` custom-format archive of the
    phoenix-postgres database alongside a tar of the mounted working
    directory.

Phoenix's mode follows ``phoenix.postgres.enabled``: ``postgres`` while
its traces, datasets and annotations live in the phoenix-postgres
database, ``volume-mount`` while they live in the SQLite database in the
working directory. A phoenix entry that names the other mode fails the
render.

In ``volume-mount`` and ``postgres`` modes the source volume is the
claim named by ``pvcName``, else the host directory on hostStorage
clusters, else the service's own StatefulSet claim.

All modes share the same upload step (mc cp + retention pruning). The
file-copy modes are not crash-consistent; the source process keeps
writing during the dump. For stronger guarantees layer CSI
VolumeSnapshots at the storage-class level. ``pg_dump`` runs in one
repeatable-read snapshot, so the database half is consistent.
*/}}
{{- /* ``default dict`` so ``--reuse-values`` from older releases doesn't
crash. Inner subkeys also defaulted explicitly because ``--reuse-values``
overlays only what the operator passed via ``--set`` on top of stored
values — chart defaults from values.yaml don't reapply. */}}
{{- $bk := (.Values.hostStorage.backup | default dict) }}
{{- if $bk.enabled }}
{{- $bucket := ($bk.bucket | default "cogniverse-backups") }}
{{- $schedule := ($bk.schedule | default "0 3 * * *") }}
{{- $retainLast := ($bk.retainLast | default 7) }}
{{- $hostStoragePath := ($.Values.hostStorage.path | default "/host-data") }}
{{- /* Default services — vespa via kubectl-exec, phoenix in the mode its
store dictates. Operator overrides as needed. */}}
{{- $defaultServices := list
    (dict "name" "vespa" "mode" "kubectl-exec" "dataPath" "/opt/vespa/var" "podLabel" "app.kubernetes.io/component=vespa")
    (dict "name" "phoenix" "hostPath" (printf "%s/phoenix" $hostStoragePath)) }}
{{- $services := ($bk.services | default $defaultServices) }}
{{- $fullName := include "cogniverse.fullname" . -}}
{{- /* Phoenix's dump mode follows where Phoenix keeps its rows. A file copy
of the working directory restores nothing while they are in Postgres, and a
pg_dump has no Service, Secret or StatefulSet to reach while they are in the
SQLite database that working directory holds. */}}
{{- $phoenixMode := ternary "postgres" "volume-mount" $.Values.phoenix.postgres.enabled }}
{{- range $svcCfg := $services }}
{{- if and (eq $svcCfg.name "phoenix") $svcCfg.mode (ne $svcCfg.mode $phoenixMode) }}
{{- if $.Values.phoenix.postgres.enabled }}
{{- fail "Phoenix uses Postgres: hostStorage.backup.services entry phoenix must use mode=postgres" }}
{{- else }}
{{- fail "Phoenix stores its data in SQLite: hostStorage.backup.services entry phoenix must use mode=volume-mount" }}
{{- end }}
{{- end }}
{{- end }}
{{- /* Backup destination — single config knob to switch between dev (in-cluster
MinIO) and cloud (R2 / B2 / AWS S3 / etc). Operator overrides:
  hostStorage.backup.s3.endpoint        — S3-compatible URL; empty = in-cluster MinIO
  hostStorage.backup.s3.existingSecret  — secret name; empty = use cogniverse-minio
  hostStorage.backup.s3.region          — required by some S3 clients (mc ignores)
The chart's default targets the in-cluster MinIO Deployment + secret, which is
fine for laptop dev IF MinIO uses ``minio.persistence.hostPath`` so its data
survives ``k3d cluster delete``. For cloud deployments, override these three
values + nothing else — same template, same code path. */}}
{{- $s3 := ($bk.s3 | default dict) }}
{{- $s3Endpoint := ($s3.endpoint | default (printf "http://%s-minio:9000" $fullName)) }}
{{- /* Secret precedence: backup.s3.existingSecret (new, explicit) →
backup.existingSecret (legacy top-level) → in-cluster MinIO secret. */}}
{{- $s3Secret := ($s3.existingSecret | default $bk.existingSecret | default (printf "%s-minio" $fullName)) }}
{{- /* Pick the runtime image (used by volume-mount mode for tar). */}}
{{- $runtimeBackend := default "cuda" $.Values.runtime.backend }}
{{- $runtimeImage := $.Values.runtime.image }}
{{- if $.Values.runtime.imagesByBackend }}
{{- $runtimeImage = (index $.Values.runtime.imagesByBackend $runtimeBackend) | default $.Values.runtime.image }}
{{- end }}
{{- /* Only render the pods/exec Role when at least one service uses kubectl-exec. */}}
{{- $needsExec := false }}
{{- range $svcCfg := $services }}
{{- $svcMode := default "kubectl-exec" $svcCfg.mode }}
{{- if eq $svcCfg.name "phoenix" }}{{- $svcMode = $phoenixMode }}{{- end }}
{{- if eq $svcMode "kubectl-exec" }}{{- $needsExec = true }}{{- end }}
{{- end }}
{{- if $needsExec }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ $fullName }}-backup-exec
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cogniverse.labels" . | nindent 4 }}
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get", "list"]
- apiGroups: [""]
  resources: ["pods/exec"]
  verbs: ["create"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ $fullName }}-backup-exec
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cogniverse.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ $fullName }}-backup-exec
subjects:
- kind: ServiceAccount
  name: {{ include "cogniverse.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
{{- end }}

{{- range $svcCfg := $services }}
{{- $svc := $svcCfg.name }}
{{- $mode := default "kubectl-exec" $svcCfg.mode }}
{{- if eq $svc "phoenix" }}{{- $mode = $phoenixMode }}{{- end }}
---
apiVersion: argoproj.io/v1alpha1
kind: CronWorkflow
metadata:
  name: {{ $fullName }}-backup-{{ $svc }}
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "cogniverse.labels" $ | nindent 4 }}
    app.kubernetes.io/component: backup
spec:
  schedule: {{ $schedule | quote }}
  timezone: "UTC"
  concurrencyPolicy: "Forbid"
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 2
  workflowSpec:
    entrypoint: snapshot
    serviceAccountName: {{ include "cogniverse.serviceAccountName" $ }}
    {{- if or (eq $mode "volume-mount") (eq $mode "postgres") }}
    {{- /* The snapshot reads the volume the source pod writes: the claim
    the operator named, the host directory on hostStorage clusters, or
    that pod's own StatefulSet claim. */}}
    volumes:
    - name: source
      {{- if $svcCfg.pvcName }}
      persistentVolumeClaim:
        claimName: {{ $svcCfg.pvcName }}
        readOnly: true
      {{- else if $.Values.hostStorage.enabled }}
      hostPath:
        path: {{ $svcCfg.hostPath | default (printf "%s/%s" $hostStoragePath $svc) }}
        type: Directory
      {{- else }}
      persistentVolumeClaim:
        claimName: {{ printf "data-%s-%s-0" $fullName $svc }}
        readOnly: true
      {{- end }}
    {{- end }}
    volumeClaimTemplates:
    - metadata:
        name: stage
      spec:
        accessModes: [ReadWriteOnce]
        resources:
          requests:
            storage: 20Gi
    templates:
    - name: snapshot
      steps:
      - - name: dump
          template: dump
      - - name: upload
          template: upload
    - name: dump
      {{- if eq $mode "kubectl-exec" }}
      container:
        image: alpine/k8s:1.31.2
        command: [sh, -c]
        args:
        - |
          set -eu
          POD=$(kubectl -n {{ $.Release.Namespace }} get pod \
            -l {{ $svcCfg.podLabel }} \
            -o jsonpath='{.items[0].metadata.name}')
          STAMP=$(date -u +%Y%m%dT%H%M%SZ)
          BASE=$(basename {{ $svcCfg.dataPath }})
          OUT=/stage/{{ $svc }}-$STAMP.tar
          echo "Snapshotting $POD:{{ $svcCfg.dataPath }} → $OUT"
          # The source is a LIVE data directory: files are rewritten and
          # removed while tar reads them, and tar reports that with exit 1
          # ("file changed as we read it"). The archive is still readable, so
          # only exit 2 and above is a failure.
          set +e
          kubectl -n {{ $.Release.Namespace }} exec "$POD" -- \
            tar -cf - -C "$(dirname {{ $svcCfg.dataPath }})" "$BASE" \
            > "$OUT"
          rc=$?
          set -e
          if [ "$rc" -ge 2 ]; then
            echo "tar failed with exit $rc" >&2
            exit "$rc"
          fi
          tar -tf "$OUT" | grep -q "^$BASE"
        env:
        - name: COGNIVERSE_INFERENCE_API_KEY
{{ include "cogniverse.inferenceApiKeyEnv" $ | nindent 10 }}
        volumeMounts:
        - name: stage
          mountPath: /stage
      {{- else if eq $mode "volume-mount" }}
      container:
        # Runtime image because it ships ``tar`` and is already pulled on
        # every cluster (the runtime pod uses it).
        image: "{{ $runtimeImage.repository }}:{{ $runtimeImage.tag }}"
        imagePullPolicy: {{ $runtimeImage.pullPolicy }}
        # UID 0 to traverse hostpath dirs that may have restrictive perms;
        # the source mount is read-only so we can't damage it.
        securityContext:
          runAsUser: 0
        command: [sh, -c]
        args:
        - |
          set -eu
          STAMP=$(date -u +%Y%m%dT%H%M%SZ)
          SNAP=/stage/snap
          rm -rf "$SNAP" && mkdir -p "$SNAP"
          echo "Snapshotting /source → /stage/{{ $svc }}-$STAMP.tar"
          cd /source
          # Tarring a live SQLite database straight off the volume fails
          # ("file changed as we read it") or worse, archives a torn copy.
          # Stage a consistent snapshot instead: .db files go through
          # SQLite's online-backup API, everything else is copied as-is
          # (-wal/-shm are folded into the backup, so they are skipped).
          find . -type d -exec mkdir -p "$SNAP/{}" \;
          find . -type f ! -name '*.db' ! -name '*.db-wal' ! -name '*.db-shm' \
            -exec cp -a {} "$SNAP/{}" \;
          find . -type f -name '*.db' | while read -r db; do
            python3 -c 'import sqlite3, sys; src = sqlite3.connect(f"file:{sys.argv[1]}?mode=ro", uri=True); dst = sqlite3.connect(sys.argv[2]); src.backup(dst); dst.close(); src.close()' "$db" "$SNAP/$db"
          done
          tar -cf "$SNAP.tar" -C "$SNAP" .
          # The upload step globs this name, so it appears only once the
          # whole archive is on disk: an interrupted dump publishes nothing.
          mv "$SNAP.tar" "/stage/{{ $svc }}-$STAMP.tar"
          rm -rf "$SNAP"
          ls -la /stage
        env:
        - name: COGNIVERSE_INFERENCE_API_KEY
{{ include "cogniverse.inferenceApiKeyEnv" $ | nindent 10 }}
        volumeMounts:
        - name: stage
          mountPath: /stage
        - name: source
          mountPath: /source
          # Not readOnly: SQLite readers of a WAL-mode database need write
          # access to the -shm file even though the backup only reads.
      {{- else if eq $mode "postgres" }}
      container:
        # The database's own client image: pg_dump and pg_restore must
        # match the server that wrote the data.
        image: "{{ $.Values.phoenix.postgres.image.repository }}:{{ $.Values.phoenix.postgres.image.tag }}"
        imagePullPolicy: {{ $.Values.phoenix.postgres.image.pullPolicy }}
        command: [sh, -c]
        args:
        - |
          set -eu
          STAMP=$(date -u +%Y%m%dT%H%M%SZ)
          WORK=/stage/work
          rm -rf "$WORK" && mkdir -p "$WORK"
          echo "Dumping $PGDATABASE on $PGHOST and /source -> /stage/{{ $svc }}-$STAMP.tar"
          pg_dump --format=custom --no-owner --no-privileges \
            --file="$WORK/database.dump"
          # pg_dump of a database that is not Phoenix's — a wiped volume, a
          # migration that never ran, a renamed phoenix.postgres.auth.database
          # — exits 0 with an archive that restores nothing, and publishing it
          # retires every good snapshot in the bucket. Read the archive back
          # and keep it only if Phoenix's own tables and rows are in it.
          pg_restore --list "$WORK/database.dump" > "$WORK/database.list"
          for table in projects traces spans datasets dataset_examples dataset_example_revisions span_annotations; do
            grep -q " TABLE DATA public $table " "$WORK/database.list" || {
              echo "archive has no $table table: $PGHOST is not Phoenix's database" >&2
              exit 1
            }
          done
          ROWS=$(pg_restore --data-only --table=projects -f - "$WORK/database.dump" \
            | awk '/^COPY /{rows=1; next} /^\\\.$/{rows=0} rows{n++} END{print n+0}')
          [ "$ROWS" -gt 0 ] || {
            echo "archive has no rows in projects: $PGDATABASE holds no Phoenix data" >&2
            exit 1
          }
          printf 'PGHOST=%s\nPGPORT=%s\nPGDATABASE=%s\nPGUSER=%s\n' \
            "$PGHOST" "$PGPORT" "$PGDATABASE" "$PGUSER" > "$WORK/restore.env"
          tar -cf "$WORK/working-assets.tar" -C /source .
          tar -cf "$WORK/archive.tar" -C "$WORK" \
            database.dump database.list restore.env working-assets.tar
          # The upload step globs this name, so it appears only once every
          # part of the snapshot is on disk: an interrupted dump publishes
          # nothing.
          mv "$WORK/archive.tar" "/stage/{{ $svc }}-$STAMP.tar"
          rm -rf "$WORK"
          ls -la /stage
        env:
        - name: PGHOST
          value: "{{ $fullName }}-phoenix-postgres"
        - name: PGPORT
          value: "5432"
        - name: PGUSER
          value: {{ $.Values.phoenix.postgres.auth.username | quote }}
        - name: PGDATABASE
          value: {{ $.Values.phoenix.postgres.auth.database | quote }}
        - name: PGPASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ $fullName }}-phoenix-postgres-auth
              key: password
        - name: COGNIVERSE_INFERENCE_API_KEY
{{ include "cogniverse.inferenceApiKeyEnv" $ | nindent 10 }}
        volumeMounts:
        - name: stage
          mountPath: /stage
        - name: source
          mountPath: /source
          readOnly: true
      {{- else }}
      {{- fail (printf "service %s has unknown mode=%s; expected kubectl-exec, volume-mount or postgres" $svc $mode) }}
      {{- end }}
    - name: upload
      container:
        image: "{{ $.Values.minio.mcImage.repository }}:{{ $.Values.minio.mcImage.tag }}{{ with $.Values.minio.mcImage.digest }}@{{ . }}{{ end }}"
        imagePullPolicy: {{ $.Values.minio.mcImage.pullPolicy }}
        env:
        - name: MINIO_ENDPOINT
          value: {{ $s3Endpoint | quote }}
        - name: MINIO_BUCKET
          value: {{ $bucket | quote }}
        # Secret keys ``rootUser`` / ``rootPassword`` match both the
        # chart's in-cluster MinIO secret AND the convention used by
        # operator-supplied secrets for cloud buckets (the operator
        # creates the secret with these key names regardless of the S3
        # provider — keeps the template provider-agnostic).
        - name: MINIO_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: {{ $s3Secret | quote }}
              key: rootUser
        - name: MINIO_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: {{ $s3Secret | quote }}
              key: rootPassword
        - name: COGNIVERSE_INFERENCE_API_KEY
{{ include "cogniverse.inferenceApiKeyEnv" $ | nindent 10 }}
        - name: RETAIN_LAST
          value: {{ $retainLast | quote }}
        # bash for parameter expansion; pgsty/mc image lacks awk + sed.
        command: [bash, -c]
        args:
        - |
          set -eu
          mc alias set dest "$MINIO_ENDPOINT" "$MINIO_ACCESS_KEY" "$MINIO_SECRET_KEY" >/dev/null
          mc mb --ignore-existing "dest/$MINIO_BUCKET"
          mc cp /stage/{{ $svc }}-*.tar "dest/$MINIO_BUCKET/{{ $svc }}/"
          # Retention: drop all but the most recent N snapshots.
          # ``mc ls`` line format: "[ts] size class filename"; ``${line##* }``
          # keeps everything after the last space → just the filename.
          mc ls "dest/$MINIO_BUCKET/{{ $svc }}/" \
            | sort -r \
            | tail -n +$((RETAIN_LAST + 1)) \
            | while IFS= read -r line; do
                fname="${line##* }"
                [ -n "$fname" ] && mc rm "dest/$MINIO_BUCKET/{{ $svc }}/$fname"
              done
        volumeMounts:
        - name: stage
          mountPath: /stage
{{- end }}
{{- end }}
